---
import cardioImage from '../assets/general-cardiology-2-scaled.jpg';
---

<div id="newBrandingModal" class="fixed inset-0 z-50 hidden">
  <div class="absolute inset-0 bg-black/70 backdrop-blur-md"></div>
  
  <div class="fixed left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2 w-full max-w-5xl">
    <div class="bg-white rounded-2xl shadow-2xl p-0 mx-4">
      <div class="relative">
        <!-- Close button -->
        <button 
          id="closeModal"
          class="absolute -right-3 -top-3 bg-white rounded-full p-2 shadow-lg hover:bg-gray-100 z-10"
        >
          <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-gray-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>

        <!-- Content -->
        <div class="grid md:grid-cols-2">
          <!-- Image Section -->
          <div class="relative h-full">
            <img 
              src={cardioImage.src} 
              alt="Heart Consultants Transformation" 
              class="w-full h-full object-cover rounded-l-2xl"
            />
            <div class="absolute inset-0 bg-gradient-to-r from-[#1a355e]/80 to-transparent rounded-l-2xl flex items-center justify-center">
              <div class="p-8 text-white">
                <h3 class="text-3xl font-bold mb-4">Heart Consultants</h3>
                <p class="text-xl opacity-90">Excellence in Cardiac Care</p>
              </div>
            </div>
          </div>

          <!-- Text Content -->
          <div class="p-8 md:p-12">
            <div class="space-y-6">
              <div class="w-16 h-16 bg-[#b71c1c]/10 rounded-full flex items-center justify-center">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 text-[#b71c1c]" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 10h4.764a2 2 0 011.789 2.894l-3.5 7A2 2 0 0115.263 21h-4.017c-.163 0-.326-.02-.485-.06L7 20m7-10V5a2 2 0 00-2-2h-.095c-.5 0-.905.405-.905.905 0 .714-.211 1.412-.608 2.006L7 11v9m7-10h-2M7 20H5a2 2 0 01-2-2v-6a2 2 0 012-2h2.5" />
                </svg>
              </div>
              
              <h2 class="text-3xl font-bold text-[#1a355e]">
                A New Chapter in Cardiac Care
              </h2>
              
              <div class="space-y-4 text-gray-600">
                <p class="text-lg">
                  We're excited to announce that Heart Consultants is undergoing a transformation! Our commitment to providing exceptional cardiac care remains unchanged, but we're enhancing your experience with:
                </p>
                
                <ul class="space-y-3">
                  <li class="flex items-start">
                    <svg class="h-6 w-6 text-[#b71c1c] mr-2 flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                    </svg>
                    <span>Modern and intuitive website design</span>
                  </li>
                  <li class="flex items-start">
                    <svg class="h-6 w-6 text-[#b71c1c] mr-2 flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                    </svg>
                    <span>Enhanced patient resources and education materials</span>
                  </li>
                  <li class="flex items-start">
                    <svg class="h-6 w-6 text-[#b71c1c] mr-2 flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                    </svg>
                    <span>Streamlined appointment scheduling</span>
                  </li>
                </ul>
              </div>
              
              <div class="pt-4">
                <button 
                  id="exploreButton"
                  class="bg-[#b71c1c] text-white px-8 py-3 rounded-full hover:bg-[#951616] transition-colors w-full md:w-auto"
                >
                  Continue to Site
                </button>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<script>
  // Show modal after 2 seconds
  setTimeout(() => {
    const modal = document.getElementById('newBrandingModal');
    if (modal) modal.classList.remove('hidden');
  }, 2000);

  // Close modal handlers
  const closeModal = () => {
    const modal = document.getElementById('newBrandingModal');
    if (modal) modal.classList.add('hidden');
  };

  document.getElementById('closeModal')?.addEventListener('click', closeModal);
  document.getElementById('exploreButton')?.addEventListener('click', closeModal);
</script> 