---
import cardioImage from '../assets/general-cardiology-2-scaled.jpg';
---

<div id="newBrandingModal" class="fixed inset-0 z-50 hidden">
  <div class="absolute inset-0 bg-black/70 backdrop-blur-md"></div>
  
  <div class="fixed left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2 w-full max-w-5xl">
    <div class="bg-white rounded-2xl shadow-2xl p-0 mx-4">
      <div class="relative">
        <!-- Close button -->
        <button 
          id="closeModal"
          class="absolute -right-3 -top-3 bg-white rounded-full p-2 shadow-lg hover:bg-gray-100 z-10"
        >
          <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-gray-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>

        <!-- Content -->
        <div class="grid md:grid-cols-2">
          <!-- Image Section -->
          <div class="relative h-full">
            <img 
              src={cardioImage.src} 
              alt="Heart Consultants Transformation" 
              class="w-full h-full object-cover rounded-l-2xl"
            />
            <div class="absolute inset-0 bg-gradient-to-r from-[#000000]/65 to-[#000000]/20 rounded-l-2xl flex items-center justify-center">
              <div class="p-8 text-white">
                <h3 class="text-3xl font-bold mb-4">Heart Consultants</h3>
                <p class="text-xl opacity-90">Excellence in Cardiac Care</p>
              </div>
            </div>
          </div>

          <!-- Text Content -->
          <div class="p-8 md:p-12">
            <div class="space-y-6">
              
              <h2 class="text-3xl font-bold text-[#1a355e]">
                A New Chapter for Our Website
              </h2>
              
              <div class="space-y-4 text-gray-600">
                <p class="text-lg">
                  We’re thrilled to announce that the Heart Consultants website is getting a fresh new look! While our dedication to delivering exceptional cardiac care remains unwavering, we’re enhancing your online experience with:
                </p>
                
                <ul class="space-y-3">
                  <li class="flex items-start">
                    <svg class="h-6 w-6 text-[#b71c1c] mr-2 flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                    </svg>
                    <span>A modern, user-friendly website design</span>
                  </li>
                  <li class="flex items-start">
                    <svg class="h-6 w-6 text-[#b71c1c] mr-2 flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                    </svg>
                    <span>Expanded patient resources and educational content</span>
                  </li>
                  <li class="flex items-start">
                    <svg class="h-6 w-6 text-[#b71c1c] mr-2 flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                    </svg>
                    <span>Simplified appointment scheduling</span>
                  </li>
                  <li class="flex items-start">
                    <svg class="h-6 w-6 text-[#b71c1c] mr-2 flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                    </svg>
                    <span>And much more to come</span>
                  </li>
                </ul>

                <div class="text-gray-600">
                  <p>
                    Stay tuned — we can’t wait to share the improved experience with you!
                  </p>
                </div>
              </div>
              
              <div class="pt-4">
                <button 
                  id="exploreButton"
                  class="bg-[#b71c1c] text-white px-8 py-3 rounded-full hover:bg-[#951616] transition-colors w-full md:w-auto"
                >
                  Continue to Site
                </button>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<script>
  // Show modal after 2 seconds
  setTimeout(() => {
    const modal = document.getElementById('newBrandingModal');
    if (modal) modal.classList.remove('hidden');
  }, 2000);

  // Close modal handlers
  const closeModal = () => {
    const modal = document.getElementById('newBrandingModal');
    if (modal) modal.classList.add('hidden');
  };

  document.getElementById('closeModal')?.addEventListener('click', closeModal);
  document.getElementById('exploreButton')?.addEventListener('click', closeModal);
</script> 